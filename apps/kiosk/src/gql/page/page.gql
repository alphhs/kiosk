query page($slug: String!) {
  page(slug: $slug) {
    id
    title
    metaTitle
    metaDescription
    metaKeywords
    metaImage {
      id
      attachmentFileName
    }
    items
    background
  }
}
